import { createContext } from "react";
import CustomThemeProvider from "../providers/CustomThemeProvider";
const CustomThemeContext = createContext(CustomThemeProvider);
export default CustomThemeContext;